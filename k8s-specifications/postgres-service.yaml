apiVersion: v1
kind: Service
metadata:
  name: db
  labels:
    name: postgres-service
    app: election-voting-app
spec:
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    name: postgres-pod
    app: election-voting-app
